[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — sends all routes to index.html so React Router works on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
